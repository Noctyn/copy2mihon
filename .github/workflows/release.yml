name: Build & Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact_name: copy2mihon-windows-amd64.exe
            binary_name: dist/copy2mihon.exe
          - os: ubuntu-latest
            artifact_name: copy2mihon-linux-amd64
            binary_name: dist/copy2mihon
          - os: macos-latest
            artifact_name: copy2mihon-macos-arm64
            binary_name: dist/copy2mihon

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          uv sync --dev

      - name: Run Tests
        run: |
          uv run pytest -v

      - name: Build Executable with PyInstaller
        run: |
          uv run pyinstaller --onefile --name copy2mihon --clean main.py

      - name: Rename Artifact
        shell: bash
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            mv dist/copy2mihon.exe dist/${{ matrix.artifact_name }}
          else
            mv dist/copy2mihon dist/${{ matrix.artifact_name }}
          fi

      - name: Upload Binary to Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/${{ matrix.artifact_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
